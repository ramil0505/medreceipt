# medreceipt
MedReceipt — A full-stack digital prescription platform with role-based access (Admin/Doctor/Patient), bcrypt password hashing, JWT session management, TOTP two-factor authentication, account lockout, rate limiting, IDOR protection, immutable signed prescriptions, PDF generation, and an append-only audit log. Built with React, Node.js, and MongoDB.
